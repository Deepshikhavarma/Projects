<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="question_marks.list">
      <field name="name">QuestionMarks list</field>
      <field name="model">question_marks.question_marks</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="question_marks.action_window">
      <field name="name">QuestionMarks window</field>
      <field name="res_model">question_marks.question_marks</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="question_marks.action_server">
      <field name="name">QuestionMarks server</field>
      <field name="model_id" ref="model_question_marks_question_marks"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="QuestionMarks" id="question_marks.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="question_marks.menu_1" parent="question_marks.menu_root"/>
    <menuitem name="Menu 2" id="question_marks.menu_2" parent="question_marks.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="question_marks.menu_1_list" parent="question_marks.menu_1"
              action="question_marks.action_window"/>
    <menuitem name="Server to list" id="question_marks" parent="question_marks.menu_2"
              action="question_marks.action_server"/>
    -->
  </data>
</odoo>